# plotters(bitmap) + iced, 3d graph demo

We have also `plotters + minifb` demo on `examples` directory.

- `$ cargo run`: run iced demo
- `$ cargo run --exmaples minifb`: run minifb demo

![screenshot](screenshot.png)
